import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';  // Puedes agregarlo si necesitas estilos
import App from './App'; // El componente principal

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
